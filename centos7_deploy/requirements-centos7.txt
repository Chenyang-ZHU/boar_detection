# ============================================================
# CentOS 7 野猪检测服务 依赖清单（pip 方式，全部钉到 glibc 2.17 兼容版本）
#
# ⚠️ 注意：
#   1. torch/torchvision 必须从 PyTorch CPU 源单独安装（见下方命令），
#      不能从普通 PyPI 装（会拉 CUDA 依赖且版本超限）。
#   2. 以下版本均为 manylinux1/2014/_2_17 构建，兼容 CentOS 7 的 glibc 2.17。
#      不要升级到更高版本（numpy 2.x / opencv 4.10+ / torch 2.7+ 需 glibc 2.28）。
#
# 安装方式（pip 路线，替代 install_centos7.sh 里的 conda 方式）:
#   python3.10 -m venv /opt/boar-detection/venv
#   /opt/boar-detection/venv/bin/pip install torch==2.6.0 torchvision==0.21.0 \
#       --index-url https://download.pytorch.org/whl/cpu
#   /opt/boar-detection/venv/bin/pip install -r requirements-centos7.txt
# ============================================================

# --- 编译型依赖（钉死，glibc 2.17 兼容）---
numpy==1.26.4
opencv-python==4.9.0.80
scipy==1.11.4
pandas==2.0.3
matplotlib==3.7.5
pillow==10.3.0

# --- 纯 Python 依赖 ---
seaborn==0.13.0
psutil==5.9.8
py-cpuinfo==9.0.0
ultralytics-thop==2.1.6
pyyaml==6.0.1
requests==2.31.0
tqdm==4.66.1

# --- 服务本体 ---
ultralytics==8.4.114
flask==3.0.3
waitress==3.0.2
qtfaststart==1.8
